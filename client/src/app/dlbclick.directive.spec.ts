import { DlbclickDirective } from './dlbclick.directive';

describe('DlbclickDirective', () => {
  it('should create an instance', () => {
    const directive = new DlbclickDirective();
    expect(directive).toBeTruthy();
  });
});
